#include<stdio.h>

int main() {

    int n,a,x,y;

    scanf("%d", &n);

    for (int i = 0; i < n; i++)
    {
        if (a <= (x * y))
        {
            printf("%d\n", n);
        }

        else {
            printf("%d\n", x);
        }
        
    }

    return 0;
    
}