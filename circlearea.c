#include<stdio.h>

//This code is used to find the afrea of the circle using the radius.

int main() {
    int radius;
    printf("Enter radius ");
    scanf("%f", &radius);

    printf("The Area of the circle is : %f", 3.14 * radius * radius);

    return 0;
}