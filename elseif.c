#include<stdio.h>

int main() {

    int n;

    printf("enter (N) : ");
    scanf("%d", &n);

    else {
        printf("It dosent Exist");
    }

    if (n %2 != 0)
    {
        printf("It is a Prime Number.")
    }
    
    return 0;

}